# 프론트엔드 어드민 통합 작업 및 백엔드 연동 가이드

## 📋 목차

1. [작업 개요](#작업-개요)
2. [통합된 컴포넌트 상세](#통합된-컴포넌트-상세)
3. [백엔드 연동 가이드](#백엔드-연동-가이드)
4. [API 엔드포인트 명세](#api-엔드포인트-명세)
5. [데이터 모델](#데이터-모델)
6. [구현 예시](#구현-예시)
7. [보안 고려사항](#보안-고려사항)

---

## 작업 개요

### 완료된 작업

**날짜**: 2025-12-03  
**브랜치**: front-sjw  
**커밋 메시지**: "21_Frontend_admin"

### 변경 사항 요약

- `25_frontend_admin` 디렉토리의 유용한 컴포넌트를 `21_frontend_admin`으로 통합
- 중복 디렉토리 제거로 프로젝트 구조 정리
- 총 5개 파일 추가 (컴포넌트 3개, 훅 1개, 유틸리티 1개)

### 통합된 파일 목록

| 파일명 | 타입 | 크기 | 위치 |
|--------|------|------|------|
| FilterPanel.tsx | 컴포넌트 | 14,224 bytes | src/components/ |
| ExportButton.tsx | 컴포넌트 | 8,474 bytes | src/components/ |
| ChartDetailModal.tsx | 컴포넌트 | 11,278 bytes | src/components/ |
| useRealtimeData.ts | Hook | 10,959 bytes | src/hooks/ |
| export.ts | Utility | 9,815 bytes | src/utils/ |

---

## 통합된 컴포넌트 상세

### 1. FilterPanel.tsx

#### 기능 설명
이상 거래 페이지를 위한 고급 필터링 컴포넌트입니다.

#### 주요 기능
- **날짜 범위 필터**: 시작일, 종료일 선택
- **위험도 필터**: 위험/경고/주의 복수 선택 가능
- **금액 범위 필터**: 최소/최대 금액 입력
- **사용자 검색**: 이름 또는 ID로 검색
- **접기/펼치기**: UI 공간 절약
- **활성 필터 카운터**: 현재 적용된 필터 개수 표시

#### Props 인터페이스
```typescript
interface FilterPanelProps {
  onFilterChange?: (filters: FilterState) => void;  // 실시간 필터 변경
  onApply: (filters: FilterState) => void;          // 필터 적용
  onReset: () => void;                              // 필터 초기화
  initialFilters?: Partial<FilterState>;             // 초기값
}

interface FilterState {
  dateFrom: string;           // YYYY-MM-DD
  dateTo: string;             // YYYY-MM-DD
  riskLevels: RiskLevel[];    // ['위험', '경고', '주의']
  amountMin: number | null;
  amountMax: number | null;
  userSearch: string;
}

type RiskLevel = '위험' | '경고' | '주의';
```

#### 의존성
- `@/types/anomaly` - RiskLevel 타입 정의 필요

---

### 2. ExportButton.tsx

#### 기능 설명
데이터를 CSV, Excel, PNG 형식으로 내보내는 드롭다운 버튼 컴포넌트입니다.

#### 주요 기능
- **CSV 내보내기**: 한글 인코딩 지원 (BOM)
- **Excel 내보내기**: 다중 시트 지원 (xlsx 패키지 필요)
- **PNG 내보내기**: 차트 캡처 (html2canvas 패키지 필요)
- **로딩 상태**: 다운로드 중 스피너 표시
- **드롭다운 UI**: 형식 선택 메뉴

#### Props 인터페이스
```typescript
interface ExportButtonProps {
  data: any[];                                     // 내보낼 데이터
  type: 'anomalies' | 'transactions' | 'dashboard'; // 데이터 타입
  filename?: string;                                // 파일명 (확장자 제외)
  showChartExport?: boolean;                        // 차트 export 옵션 표시
  chartElementId?: string;                          // 차트 HTML ID
}
```

#### 필요한 패키지
```bash
npm install xlsx html2canvas
```

#### 의존성
- `@/utils/export` - 내보내기 유틸리티 함수

---

### 3. ChartDetailModal.tsx

#### 기능 설명
차트 요소 클릭 시 상세 데이터를 모달로 표시하는 컴포넌트입니다.

#### 주요 기능
- **날짜별 상세**: 특정 날짜의 전체 거래 목록
- **카테고리별 상세**: 특정 카테고리의 전체 거래 목록
- **ESC 키 닫기**: 키보드 단축키 지원
- **오버레이 클릭 닫기**: 배경 클릭으로 닫기
- **스크롤 지원**: 많은 데이터도 표시 가능
- **body 스크롤 방지**: 모달 열림 시 배경 스크롤 차단

#### Props 인터페이스
```typescript
interface ChartDetailModalProps {
  isOpen: boolean;                    // 모달 표시 여부
  onClose: () => void;                // 닫기 콜백
  data: ChartDetailData | null;       // 차트 상세 데이터
}

interface ChartDetailData {
  type: 'date' | 'category';
  label: string;                      // 제목
  items: Array<{
    id: string | number;
    description: string;
    amount: number;
    date?: string;
    category?: string;
    [key: string]: any;
  }>;
  total: number;                      // 총 합계
  count: number;                      // 거래 건수
}
```

#### 의존성
- `@/utils/format` - formatCurrency, formatDate 함수 필요

---

### 4. useRealtimeData.ts

#### 기능 설명
주기적으로 백엔드에서 데이터를 폴링하여 실시간 업데이트를 제공하는 커스텀 훅입니다.

#### 주요 기능
- **자동 폴링**: 설정된 간격으로 데이터 fetch
- **새 데이터 감지**: 데이터 변경 시 알림
- **일시정지/재개**: 사용자 제어 가능
- **수동 새로고침**: 즉시 데이터 갱신
- **에러 처리**: fetch 실패 시 콜백
- **컴포넌트 언마운트 처리**: 메모리 누수 방지

#### 사용 예시
```typescript
const {
  data,           // 현재 데이터
  isLoading,      // 로딩 상태
  error,          // 에러
  newDataCount,   // 새 데이터 개수
  refresh,        // 수동 새로고침
  pause,          // 일시정지
  resume,         // 재개
  isPaused        // 일시정지 상태
} = useRealtimeData('/api/anomalies/pending', {
  intervalMs: 30000,        // 30초마다
  enabled: true,
  onNewData: (count) => {
    console.log(`새 데이터 ${count}건`);
  },
  onError: (error) => {
    console.error('에러:', error);
  }
});
```

#### 폴링 설정
- 기본 간격: 30초 (30000ms)
- 최초 실행: 즉시 1회 실행 후 폴링 시작
- 백그라운드 폴링: 로딩 표시 없음

---

### 5. export.ts

#### 기능 설명
데이터를 다양한 형식으로 내보내는 유틸리티 함수 모음입니다.

#### 제공 함수

##### exportToCSV
```typescript
exportToCSV<T extends Record<string, any>>(
  data: T[],
  type: 'anomalies' | 'transactions' | 'dashboard',
  filename?: string
): void
```
- CSV 파일로 내보내기
- BOM 추가로 한글 깨짐 방지
- 쉼표/줄바꿈 이스케이프 처리

##### exportToExcel
```typescript
exportToExcel(
  sheets: Record<string, any[]>,
  filename?: string
): void
```
- Excel 파일로 내보내기 (다중 시트 지원)
- **주의**: `xlsx` 패키지 설치 필요
- 현재는 경고 메시지만 표시 (구현 후 주석 해제)

##### exportChartToPNG
```typescript
exportChartToPNG(
  elementId: string,
  filename?: string,
  scale?: number
): Promise<void>
```
- 차트를 PNG 이미지로 저장
- **주의**: `html2canvas` 패키지 설치 필요
- scale: 이미지 해상도 (기본 2배)

---

## 백엔드 연동 가이드

### 필요한 타입 정의 파일 생성

#### 1. src/types/anomaly.ts
```typescript
/**
 * 이상 거래 위험도 레벨
 */
export type RiskLevel = '위험' | '경고' | '주의';

/**
 * 이상 거래 데이터 인터페이스
 */
export interface Anomaly {
  id: string | number;
  userId: string;
  userName: string;
  merchant: string;
  amount: number;
  category: string;
  transactionDate: string;     // ISO 8601 format
  riskLevel: RiskLevel;
  riskScore: number;            // 0-100
  reason: string;               // 이상 거래로 판단된 이유
  status: 'pending' | 'approved' | 'rejected';
  createdAt: string;
  updatedAt: string;
}

/**
 * 이상 거래 목록 응답
 */
export interface AnomaliesResponse {
  data: Anomaly[];
  total: number;
  page: number;
  pageSize: number;
  timestamp: string;
}
```

#### 2. src/utils/format.ts
```typescript
/**
 * 금액을 한국 원화 형식으로 포맷
 */
export function formatCurrency(amount: number): string {
  return new Intl.NumberFormat('ko-KR', {
    style: 'currency',
    currency: 'KRW',
  }).format(amount);
}

/**
 * 날짜를 읽기 쉬운 형식으로 포맷
 */
export function formatDate(dateString: string): string {
  const date = new Date(dateString);
  
  return new Intl.DateTimeFormat('ko-KR', {
    year: 'numeric',
    month: 'long',
    day: 'numeric',
    hour: '2-digit',
    minute: '2-digit',
  }).format(date);
}

/**
 * 상대적 시간 표시 (예: "5분 전")
 */
export function formatRelativeTime(dateString: string): string {
  const date = new Date(dateString);
  const now = new Date();
  const diffMs = now.getTime() - date.getTime();
  const diffMins = Math.floor(diffMs / 60000);
  const diffHours = Math.floor(diffMs / 3600000);
  const diffDays = Math.floor(diffMs / 86400000);

  if (diffMins < 1) return '방금 전';
  if (diffMins < 60) return `${diffMins}분 전`;
  if (diffHours < 24) return `${diffHours}시간 전`;
  if (diffDays < 7) return `${diffDays}일 전`;
  
  return formatDate(dateString);
}
```

---

## API 엔드포인트 명세

### 1. 이상 거래 목록 조회

#### GET /api/anomalies

**쿼리 파라미터:**
```typescript
{
  date_from?: string;      // YYYY-MM-DD
  date_to?: string;        // YYYY-MM-DD
  risk_levels?: string;    // 쉼표 구분 (예: "위험,경고")
  amount_min?: number;
  amount_max?: number;
  user_search?: string;    // 사용자 이름 또는 ID
  page?: number;           // 페이지 번호 (기본: 1)
  page_size?: number;      // 페이지 크기 (기본: 20)
  status?: string;         // pending, approved, rejected
}
```

**응답 예시:**
```json
{
  "data": [
    {
      "id": "anom_123456",
      "userId": "user_789",
      "userName": "홍길동",
      "merchant": "해외 온라인 쇼핑몰",
      "amount": 2500000,
      "category": "쇼핑",
      "transactionDate": "2025-12-03T10:30:00Z",
      "riskLevel": "위험",
      "riskScore": 92,
      "reason": "평균 거래액 대비 10배 초과",
      "status": "pending",
      "createdAt": "2025-12-03T10:35:00Z",
      "updatedAt": "2025-12-03T10:35:00Z"
    }
  ],
  "total": 156,
  "page": 1,
  "pageSize": 20,
  "timestamp": "2025-12-03T11:00:00Z"
}
```

**FastAPI 구현 예시:**
```python
from fastapi import APIRouter, Query
from typing import Optional, List
from datetime import datetime

router = APIRouter()

@router.get("/api/anomalies")
async def get_anomalies(
    date_from: Optional[str] = Query(None),
    date_to: Optional[str] = Query(None),
    risk_levels: Optional[str] = Query(None),
    amount_min: Optional[float] = Query(None),
    amount_max: Optional[float] = Query(None),
    user_search: Optional[str] = Query(None),
    page: int = Query(1, ge=1),
    page_size: int = Query(20, ge=1, le=100),
    status: Optional[str] = Query("pending")
):
    # 쿼리 빌딩
    query = db.query(Anomaly).filter(Anomaly.status == status)
    
    # 날짜 필터
    if date_from:
        query = query.filter(Anomaly.transaction_date >= date_from)
    if date_to:
        query = query.filter(Anomaly.transaction_date <= date_to)
    
    # 위험도 필터
    if risk_levels:
        levels = risk_levels.split(',')
        query = query.filter(Anomaly.risk_level.in_(levels))
    
    # 금액 필터
    if amount_min is not None:
        query = query.filter(Anomaly.amount >= amount_min)
    if amount_max is not None:
        query = query.filter(Anomaly.amount <= amount_max)
    
    # 사용자 검색
    if user_search:
        query = query.filter(
            or_(
                Anomaly.user_name.ilike(f"%{user_search}%"),
                Anomaly.user_id.ilike(f"%{user_search}%")
            )
        )
    
    # 총 개수 조회
    total = query.count()
    
    # 페이지네이션
    offset = (page - 1) * page_size
    anomalies = query.offset(offset).limit(page_size).all()
    
    return {
        "data": anomalies,
        "total": total,
        "page": page,
        "pageSize": page_size,
        "timestamp": datetime.utcnow().isoformat()
    }
```

---

### 2. 실시간 업데이트를 위한 폴링 엔드포인트

#### GET /api/anomalies/pending

**설명**: 30초마다 호출되어 새로운 대기 중인 이상 거래를 확인합니다.

**응답 예시:**
```json
{
  "data": [
    {
      "id": "anom_999",
      "userId": "user_555",
      "userName": "김철수",
      "merchant": "해외 송금",
      "amount": 5000000,
      "category": "송금",
      "transactionDate": "2025-12-03T10:55:00Z",
      "riskLevel": "위험",
      "riskScore": 95,
      "reason": "첫 해외 송금 + 고액",
      "status": "pending",
      "createdAt": "2025-12-03T10:56:00Z",
      "updatedAt": "2025-12-03T10:56:00Z"
    }
  ],
  "timestamp": "2025-12-03T11:00:00Z",
  "count": 1
}
```

**FastAPI 구현 예시:**
```python
@router.get("/api/anomalies/pending")
async def get_pending_anomalies():
    anomalies = db.query(Anomaly).filter(
        Anomaly.status == "pending"
    ).order_by(Anomaly.created_at.desc()).all()
    
    return {
        "data": anomalies,
        "timestamp": datetime.utcnow().isoformat(),
        "count": len(anomalies)
    }
```

---

### 3. 차트 상세 데이터 조회

#### GET /api/transactions

**용도**: 차트 클릭 시 해당 기간/카테고리의 상세 거래 내역 조회

**쿼리 파라미터:**
```typescript
{
  month?: string;        // YYYY-MM (월별 조회)
  date?: string;         // YYYY-MM-DD (일별 조회)
  category?: string;     // 카테고리별 조회
  user_id?: string;      // 특정 사용자 조회
}
```

**응답 예시:**
```json
{
  "items": [
    {
      "id": "trans_12345",
      "description": "스타벅스 강남점",
      "amount": 15000,
      "date": "2025-12-03T09:30:00Z",
      "category": "식비",
      "merchant": "스타벅스",
      "paymentMethod": "신용카드"
    }
  ],
  "total": 150000,
  "count": 10
}
```

**FastAPI 구현 예시:**
```python
@router.get("/api/transactions")
async def get_transactions(
    month: Optional[str] = Query(None),
    date: Optional[str] = Query(None),
    category: Optional[str] = Query(None),
    user_id: Optional[str] = Query(None)
):
    query = db.query(Transaction)
    
    # 월별 조회
    if month:
        year, month_num = month.split('-')
        query = query.filter(
            extract('year', Transaction.transaction_date) == int(year),
            extract('month', Transaction.transaction_date) == int(month_num)
        )
    
    # 일별 조회
    if date:
        query = query.filter(
            func.date(Transaction.transaction_date) == date
        )
    
    # 카테고리별 조회
    if category:
        query = query.filter(Transaction.category == category)
    
    # 사용자별 조회
    if user_id:
        query = query.filter(Transaction.user_id == user_id)
    
    transactions = query.all()
    total_amount = sum(t.amount for t in transactions)
    
    return {
        "items": [
            {
                "id": t.id,
                "description": t.merchant,
                "amount": t.amount,
                "date": t.transaction_date.isoformat(),
                "category": t.category,
                "merchant": t.merchant,
                "paymentMethod": t.payment_method
            }
            for t in transactions
        ],
        "total": total_amount,
        "count": len(transactions)
    }
```

---

### 4. 데이터 내보내기 (서버 사이드)

#### POST /api/export

**설명**: 대용량 데이터 내보내기는 서버에서 처리하는 것이 효율적입니다.

**요청 바디:**
```json
{
  "type": "csv",
  "dataType": "anomalies",
  "filters": {
    "date_from": "2025-11-01",
    "date_to": "2025-12-03",
    "risk_levels": ["위험", "경고"]
  }
}
```

**응답**: 파일 스트림 (Content-Disposition: attachment)

**FastAPI 구현 예시:**
```python
from fastapi.responses import StreamingResponse
import csv
import io

@router.post("/api/export")
async def export_data(request: ExportRequest):
    # 필터 적용하여 데이터 조회
    query = build_query_from_filters(request.filters)
    data = query.all()
    
    # CSV 생성
    if request.type == "csv":
        output = io.StringIO()
        # BOM 추가 (한글 깨짐 방지)
        output.write('\ufeff')
        
        writer = csv.DictWriter(output, fieldnames=get_csv_headers(request.dataType))
        writer.writeheader()
        
        for item in data:
            writer.writerow(item.to_dict())
        
        output.seek(0)
        
        return StreamingResponse(
            iter([output.getvalue()]),
            media_type="text/csv",
            headers={
                "Content-Disposition": f'attachment; filename="export_{datetime.now().strftime("%Y%m%d")}.csv"'
            }
        )
    
    # Excel 생성
    elif request.type == "excel":
        # openpyxl 또는 xlsxwriter 사용
        # ... Excel 생성 로직
        pass
```

---

## 데이터 모델

### 데이터베이스 스키마 (SQLAlchemy 예시)

```python
from sqlalchemy import Column, Integer, String, Float, DateTime, Enum
from sqlalchemy.ext.declarative import declarative_base
from datetime import datetime
import enum

Base = declarative_base()

class RiskLevelEnum(enum.Enum):
    HIGH = "위험"
    MEDIUM = "경고"
    LOW = "주의"

class StatusEnum(enum.Enum):
    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"

class Anomaly(Base):
    __tablename__ = "anomalies"
    
    id = Column(String, primary_key=True)
    user_id = Column(String, nullable=False, index=True)
    user_name = Column(String, nullable=False)
    merchant = Column(String, nullable=False)
    amount = Column(Float, nullable=False)
    category = Column(String, nullable=False)
    transaction_date = Column(DateTime, nullable=False, index=True)
    risk_level = Column(Enum(RiskLevelEnum), nullable=False, index=True)
    risk_score = Column(Float, nullable=False)
    reason = Column(String, nullable=False)
    status = Column(Enum(StatusEnum), nullable=False, default=StatusEnum.PENDING, index=True)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow)
    updated_at = Column(DateTime, nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # 인덱스
    __table_args__ = (
        Index('idx_status_created', 'status', 'created_at'),
        Index('idx_transaction_date', 'transaction_date'),
    )

class Transaction(Base):
    __tablename__ = "transactions"
    
    id = Column(String, primary_key=True)
    user_id = Column(String, nullable=False, index=True)
    merchant = Column(String, nullable=False)
    amount = Column(Float, nullable=False)
    category = Column(String, nullable=False, index=True)
    transaction_date = Column(DateTime, nullable=False, index=True)
    payment_method = Column(String)
    created_at = Column(DateTime, nullable=False, default=datetime.utcnow)
```

---

## 구현 예시

### 이상 거래 페이지 전체 통합 예시

```typescript
// pages/anomalies.tsx
import { useState } from 'react';
import FilterPanel, { FilterState } from '@/components/FilterPanel';
import ExportButton from '@/components/ExportButton';
import ChartDetailModal, { ChartDetailData } from '@/components/ChartDetailModal';
import { useRealtimeData } from '@/hooks/useRealtimeData';
import { Anomaly } from '@/types/anomaly';

export default function AnomaliesPage() {
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // State
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const [filters, setFilters] = useState<FilterState>({
    dateFrom: '',
    dateTo: '',
    riskLevels: [],
    amountMin: null,
    amountMax: null,
    userSearch: '',
  });
  
  const [anomalies, setAnomalies] = useState<Anomaly[]>([]);
  const [modalData, setModalData] = useState<ChartDetailData | null>(null);

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 실시간 데이터 폴링
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const {
    data: realtimeData,
    newDataCount,
    refresh: refreshRealtime,
  } = useRealtimeData<Anomaly[]>('/api/anomalies/pending', {
    intervalMs: 30000,
    onNewData: (count) => {
      // 토스트 알림 표시
      showToast(`새로운 이상 거래 ${count}건이 감지되었습니다.`);
    },
  });

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 필터 적용
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const handleApplyFilters = async (newFilters: FilterState) => {
    setFilters(newFilters);
    
    // URL 파라미터 생성
    const params = new URLSearchParams();
    if (newFilters.dateFrom) params.append('date_from', newFilters.dateFrom);
    if (newFilters.dateTo) params.append('date_to', newFilters.dateTo);
    if (newFilters.riskLevels.length) {
      params.append('risk_levels', newFilters.riskLevels.join(','));
    }
    if (newFilters.amountMin !== null) {
      params.append('amount_min', String(newFilters.amountMin));
    }
    if (newFilters.amountMax !== null) {
      params.append('amount_max', String(newFilters.amountMax));
    }
    if (newFilters.userSearch) {
      params.append('user_search', newFilters.userSearch);
    }
    
    // API 호출
    try {
      const response = await fetch(`/api/anomalies?${params}`);
      const data = await response.json();
      setAnomalies(data.data);
    } catch (error) {
      console.error('필터 적용 실패:', error);
      showToast('데이터 로딩에 실패했습니다.', 'error');
    }
  };

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 차트 클릭 핸들러
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  const handleChartClick = async (clickedData: any) => {
    try {
      // 해당 날짜/카테고리의 상세 데이터 조회
      const response = await fetch(
        `/api/transactions?date=${clickedData.date}`
      );
      const details = await response.json();
      
      setModalData({
        type: 'date',
        label: `${clickedData.date} 거래 내역`,
        items: details.items,
        total: details.total,
        count: details.count,
      });
    } catch (error) {
      console.error('상세 데이터 조회 실패:', error);
    }
  };

  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  // 렌더링
  // ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  return (
    <div className="p-6">
      {/* 헤더 */}
      <div className="flex justify-between items-center mb-6">
        <div>
          <h1 className="text-3xl font-bold">이상 거래 모니터링</h1>
          {newDataCount > 0 && (
            <p className="text-sm text-red-600 mt-2">
              ⚠️ 새로운 이상 거래 {newDataCount}건
            </p>
          )}
        </div>
        
        <ExportButton
          data={anomalies}
          type="anomalies"
          filename={`이상거래_${new Date().toISOString().split('T')[0]}`}
        />
      </div>

      {/* 필터 패널 */}
      <FilterPanel
        onApply={handleApplyFilters}
        onReset={() => {
          setFilters({
            dateFrom: '',
            dateTo: '',
            riskLevels: [],
            amountMin: null,
            amountMax: null,
            userSearch: '',
          });
          handleApplyFilters({
            dateFrom: '',
            dateTo: '',
            riskLevels: [],
            amountMin: null,
            amountMax: null,
            userSearch: '',
          });
        }}
      />

      {/* 차트 (Recharts 등 사용) */}
      <div className="mt-6">
        <BarChart
          data={chartData}
          onClick={handleChartClick}
        />
      </div>

      {/* 이상 거래 목록 */}
      <div className="mt-6">
        <AnomalyTable data={anomalies} />
      </div>

      {/* 차트 상세 모달 */}
      <ChartDetailModal
        isOpen={!!modalData}
        data={modalData}
        onClose={() => setModalData(null)}
      />
    </div>
  );
}
```

---

## 보안 고려사항

### 1. 인증 및 권한 확인

모든 API 엔드포인트에 인증 미들웨어를 적용해야 합니다.

```python
from fastapi import Depends, HTTPException, status
from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials

security = HTTPBearer()

async def verify_admin(
    credentials: HTTPAuthorizationCredentials = Depends(security)
):
    """관리자 권한 확인"""
    token = credentials.credentials
    
    # JWT 토큰 검증
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=["HS256"])
        user_role = payload.get("role")
        
        if user_role != "admin":
            raise HTTPException(
                status_code=status.HTTP_403_FORBIDDEN,
                detail="관리자 권한이 필요합니다"
            )
        
        return payload
    except jwt.JWTError:
        raise HTTPException(
            status_code=status.HTTP_401_UNAUTHORIZED,
            detail="유효하지 않은 토큰입니다"
        )

# 사용 예시
@router.get("/api/anomalies")
async def get_anomalies(
    admin = Depends(verify_admin),
    # ... 기타 파라미터
):
    # ...
```

### 2. Rate Limiting

폴링 엔드포인트에 Rate Limiting을 적용하여 과도한 요청을 방지합니다.

```python
from slowapi import Limiter
from slowapi.util import get_remote_address

limiter = Limiter(key_func=get_remote_address)

@router.get("/api/anomalies/pending")
@limiter.limit("2/minute")  # 분당 2회로 제한
async def get_pending_anomalies(request: Request):
    # ...
```

### 3. 데이터 마스킹

민감한 개인 정보는 마스킹 처리합니다.

```python
def mask_sensitive_data(anomaly: Anomaly) -> dict:
    """민감 정보 마스킹"""
    return {
        "id": anomaly.id,
        "userId": anomaly.user_id,
        "userName": mask_name(anomaly.user_name),  # "홍*동"
        "merchant": anomaly.merchant,
        "amount": anomaly.amount,
        # ... 기타 필드
    }

def mask_name(name: str) -> str:
    """이름 마스킹"""
    if len(name) <= 2:
        return name[0] + "*"
    return name[0] + "*" * (len(name) - 2) + name[-1]
```

### 4. SQL Injection 방지

ORM 사용 시 자동으로 방지되지만, 직접 쿼리 작성 시 주의가 필요합니다.

```python
# ❌ 위험: SQL Injection 취약
query = f"SELECT * FROM anomalies WHERE user_name LIKE '%{user_search}%'"

# ✅ 안전: 파라미터화된 쿼리
query = db.query(Anomaly).filter(
    Anomaly.user_name.ilike(f"%{user_search}%")
)
```

### 5. CORS 설정

프론트엔드 도메인에서만 API 접근을 허용합니다.

```python
from fastapi.middleware.cors import CORSMiddleware

app.add_middleware(
    CORSMiddleware,
    allow_origins=[
        "http://localhost:3000",
        "https://yourdomain.com"
    ],
    allow_credentials=True,
    allow_methods=["GET", "POST", "PUT", "DELETE"],
    allow_headers=["*"],
)
```

### 6. 감사 로그

모든 관리자 작업을 로깅합니다.

```python
async def log_admin_action(
    admin_id: str,
    action: str,
    target_id: str,
    details: dict
):
    """관리자 작업 로그 기록"""
    audit_log = AuditLog(
        admin_id=admin_id,
        action=action,
        target_id=target_id,
        details=json.dumps(details),
        timestamp=datetime.utcnow(),
        ip_address=request.client.host
    )
    db.add(audit_log)
    db.commit()
```

---

## 성능 최적화

### 1. 데이터베이스 인덱스

자주 조회되는 컬럼에 인덱스를 추가합니다.

```sql
CREATE INDEX idx_anomalies_status_created ON anomalies(status, created_at DESC);
CREATE INDEX idx_anomalies_transaction_date ON anomalies(transaction_date);
CREATE INDEX idx_anomalies_risk_level ON anomalies(risk_level);
CREATE INDEX idx_anomalies_user_id ON anomalies(user_id);
```

### 2. 쿼리 최적화

```python
# ❌ N+1 문제
anomalies = db.query(Anomaly).all()
for anomaly in anomalies:
    user = db.query(User).filter(User.id == anomaly.user_id).first()

# ✅ JOIN 사용
anomalies = db.query(Anomaly).join(User).all()
```

### 3. 캐싱

자주 조회되는 데이터는 Redis 등을 사용하여 캐싱합니다.

```python
from redis import Redis
import json

redis_client = Redis(host='localhost', port=6379)

@router.get("/api/anomalies/stats")
async def get_anomaly_stats():
    # 캐시 확인
    cached = redis_client.get("anomaly_stats")
    if cached:
        return json.loads(cached)
    
    # DB 조회
    stats = calculate_anomaly_stats()
    
    # 캐시 저장 (5분 TTL)
    redis_client.setex(
        "anomaly_stats",
        300,
        json.dumps(stats)
    )
    
    return stats
```

---

## 테스트

### 프론트엔드 컴포넌트 테스트

```typescript
// FilterPanel.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import FilterPanel from '@/components/FilterPanel';

describe('FilterPanel', () => {
  it('필터 적용 시 콜백이 호출되어야 함', () => {
    const onApply = jest.fn();
    const onReset = jest.fn();
    
    render(<FilterPanel onApply={onApply} onReset={onReset} />);
    
    // 필터 입력
    const dateFromInput = screen.getByPlaceholderText('시작일');
    fireEvent.change(dateFromInput, { target: { value: '2025-12-01' } });
    
    // 적용 버튼 클릭
    const applyButton = screen.getByText('필터 적용');
    fireEvent.click(applyButton);
    
    // 콜백 확인
    expect(onApply).toHaveBeenCalledWith(
      expect.objectContaining({
        dateFrom: '2025-12-01'
      })
    );
  });
});
```

### 백엔드 API 테스트

```python
from fastapi.testclient import TestClient

def test_get_anomalies_with_filters():
    """필터링된 이상 거래 조회 테스트"""
    response = client.get(
        "/api/anomalies",
        params={
            "risk_levels": "위험,경고",
            "amount_min": 100000
        }
    )
    
    assert response.status_code == 200
    data = response.json()
    
    # 모든 결과가 필터 조건을 만족하는지 확인
    for anomaly in data["data"]:
        assert anomaly["riskLevel"] in ["위험", "경고"]
        assert anomaly["amount"] >= 100000
```

---

## 다음 단계

1. **타입 정의 파일 생성**: `src/types/anomaly.ts`, `src/utils/format.ts`
2. **백엔드 API 구현**: FastAPI 엔드포인트 개발
3. **데이터베이스 마이그레이션**: 스키마 생성 및 인덱스 추가
4. **인증 시스템 통합**: JWT 토큰 기반 인증
5. **테스트 작성**: 단위 테스트 및 통합 테스트
6. **성능 모니터링**: APM 도구 연동 (Sentry, DataDog 등)
7. **배포**: Docker 컨테이너화 및 CI/CD 파이프라인 구축

---

## 문의 및 지원

추가 질문이나 구현 중 문제가 발생하면 다음을 참고하세요:

- **프론트엔드 컴포넌트**: 각 파일 상단의 JSDoc 주석 참조
- **백엔드 API**: FastAPI 자동 생성 문서 (`/docs`) 참조
- **타입 정의**: TypeScript 타입 선언 파일 참조

---

**작성일**: 2025-12-03  
**작성자**: AI Assistant  
**버전**: 1.0.0
