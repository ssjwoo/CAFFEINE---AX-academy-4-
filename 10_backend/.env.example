# 환경 변수 템플릿
# .env.example - 실제 사용 시 .env 파일로 복사하여 값 설정

# 데이터베이스
DATABASE_URL=postgresql://user:password@localhost:5432/caffeine_db

# JWT 설정
SECRET_KEY=your-secret-key-here-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# PII 암호화 (Fernet)
ENCRYPTION_KEY=your-fernet-key-here-generate-with-Fernet.generate_key()

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006,http://localhost:5173

# 환경
ENVIRONMENT=development
