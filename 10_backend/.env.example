# 백엔드 환경 변수 예제
# 이 파일을 복사하여 .env 로 저장하고 실제 값을 입력하세요

# 데이터베이스 연결 (AWS RDS)
DATABASE_URL=postgresql+asyncpg://postgres:password@caffeine-database.xxx.rds.amazonaws.com:5432/caffeine_db

# 로컬 DB (폴백용)
LOCAL_DATABASE_URL=postgresql+asyncpg://postgres:postgres@db:5432/caffeine_db

# Gemini API 키
GEMINI_API_KEY=your_gemini_api_key_here

# JWT 설정
SECRET_KEY=your_super_secret_key_for_jwt_signing
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 암호화 키 (Fernet)
ENCRYPTION_KEY=your_fernet_encryption_key_here

# 이메일 서비스 (선택)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_app_password
